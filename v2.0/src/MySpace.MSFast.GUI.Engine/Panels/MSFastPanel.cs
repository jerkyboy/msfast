using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Forms;
using ComIterop.Win32;

namespace MySpace.Performance.Tracker.Collectors.Client.MSFast.Engine.GUI.Panels
{
    public class MSFastPanel : Panel
    {
        /*
        protected override void WndProc(ref Message m)
        {
            if (m.Msg == (int)Win32API.WindowsMessages.WM_PAINT ||
                m.Msg == (int)Win32API.WindowsMessages.WM_NCPAINT ||
                m.Msg == (int)Win32API.WindowsMessages.WM_SETREDRAW ||
                m.Msg == (int)Win32API.WindowsMessages.WM_ERASEBKGND)

                return;

            base.WndProc(ref m);
        }
         */
       
    }
}
